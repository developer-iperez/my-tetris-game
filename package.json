{
  "name": "my-tetris-game",
  "version": "0.1.0",
  "private": true,
  "description": "Tetris en el navegador (motor desacoplado, Canvas 2D, PWA).",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "npm run build && python3 -m http.server 8080"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
